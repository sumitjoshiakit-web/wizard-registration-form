export default function Field({id,label,error,type="text",register,description,children}){return <div className="field"><label htmlFor={id}>{label}</label>{description&&<span id={`${id}-hint`} className="hint">{description}</span>}{children||<input id={id} type={type} aria-invalid={!!error} aria-describedby={error?`${id}-error`:description?`${id}-hint`:undefined} {...register}/>} {error&&<span id={`${id}-error`} className="error" role="alert">{error}</span>}</div>}