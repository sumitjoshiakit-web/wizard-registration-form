export default function ReviewStep({data}){const labels={firstName:"First name",lastName:"Last name",email:"Email",phone:"Phone",username:"Username"};return <section aria-labelledby="review-heading"><h2 id="review-heading">Review & Submit</h2><p className="section-copy">Review your information before creating the account.</p><dl className="review-list">{Object.entries(labels).map(([key,label])=><div key={key}><dt>{label}</dt><dd>{data[key]||"—"}</dd></div>)}</dl><p className="secure-note">Your password is intentionally not displayed on this review screen.</p></section>}