import {useState} from "react"; import Field from "./Field.jsx";
export default function AccountStep({register,errors}){const [show,setShow]=useState(false);return <section aria-labelledby="account-heading"><h2 id="account-heading">Account Details</h2><p className="section-copy">Create the credentials you will use to sign in.</p><Field id="username" label="Username" error={errors.username?.message} register={register("username")}/><Field id="password" label="Password" type={show?"text":"password"} error={errors.password?.message} description="Minimum 8 characters, including an uppercase letter and a number." register={register("password")}/><button type="button" className="text-button" onClick={()=>setShow(v=>!v)} aria-pressed={show} aria-controls="password">{show?"Hide password":"Show password"}</button><Field id="confirmPassword" label="Confirm password" type={show?"text":"password"} error={errors.confirmPassword?.message} register={register("confirmPassword")}/><label className="checkbox"><input type="checkbox" {...register("terms")}/><span>I agree to the terms and conditions.</span></label>{errors.terms&&<span className="error" role="alert">{errors.terms.message}</span>}</section>}