export default function ProgressBar({step}){return <div className="progress-wrap" aria-label={`Registration progress: step ${step} of 3`}><div className="progress-track"><div className="progress-fill" style={{width:`${(step/3)*100}%`}}/></div><div className="steps" aria-hidden="true"><span className={step>=1?"active":""}>1. Personal</span><span className={step>=2?"active":""}>2. Account</span><span className={step>=3?"active":""}>3. Review</span></div></div>}